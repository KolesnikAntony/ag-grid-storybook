// import { useTheme } from '@mui/material/styles';

const useStyle = () => {
  // const theme = useTheme();

  return {
    menu: {
      // opacity: 0,
      position: 'absolute',
      inset: 0,
      cursor: 'pointer',
    },
  };
};

export default useStyle;
